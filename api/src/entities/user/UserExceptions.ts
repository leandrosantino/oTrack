enum a {
  VALIDATION_ERROR = 'VALIDATION_ERROR',
  NOT_FOUND = 'NOT_FOUND',
}

export enum UserCreateExceptions {
  USER_ALREADY_EXISTS = 'USER_ALREADY_EXISTS'
}


export type UserExceptions = UserCreateExceptions
